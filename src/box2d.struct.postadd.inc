// this file is generated via daScript automatic C++ binder
// all user modifications will be lost after this file is re-generated

initRecAnnotation(ann_b2BodyUserData,lib);
initRecAnnotation(ann_b2FixtureUserData,lib);
initRecAnnotation(ann_b2JointUserData,lib);
initRecAnnotation(ann_b2Version,lib);
initRecAnnotation(ann_b2Vec2,lib);
initRecAnnotation(ann_b2Vec3,lib);
initRecAnnotation(ann_b2Mat22,lib);
initRecAnnotation(ann_b2Mat33,lib);
initRecAnnotation(ann_b2Rot,lib);
initRecAnnotation(ann_b2Transform,lib);
initRecAnnotation(ann_b2Sweep,lib);
initRecAnnotation(ann_b2Color,lib);
initRecAnnotation(ann_b2Draw,lib);
initRecAnnotation(ann_b2Timer,lib);
initRecAnnotation(ann_b2MassData,lib);
initRecAnnotation(ann_b2Shape,lib);
initRecAnnotation(ann_b2ChainShape,lib);
initRecAnnotation(ann_b2CircleShape,lib);
initRecAnnotation(ann_b2EdgeShape,lib);
initRecAnnotation(ann_b2PolygonShape,lib);
initRecAnnotation(ann_b2TreeNode,lib);
initRecAnnotation(ann_b2DynamicTree,lib);
initRecAnnotation(ann_b2Pair,lib);
initRecAnnotation(ann_b2BroadPhase,lib);
initRecAnnotation(ann_b2BodyDef,lib);
initRecAnnotation(ann_b2Body,lib);
initRecAnnotation(ann_b2Filter,lib);
initRecAnnotation(ann_b2FixtureDef,lib);
initRecAnnotation(ann_b2FixtureProxy,lib);
initRecAnnotation(ann_b2Fixture,lib);
initRecAnnotation(ann_b2ContactRegister,lib);
initRecAnnotation(ann_b2ContactEdge,lib);
initRecAnnotation(ann_b2Contact,lib);
initRecAnnotation(ann_b2Profile,lib);
initRecAnnotation(ann_b2SolverData,lib);
initRecAnnotation(ann_b2BlockAllocator,lib);
initRecAnnotation(ann_b2ContactManager,lib);
initRecAnnotation(ann_b2StackEntry,lib);
initRecAnnotation(ann_b2StackAllocator,lib);
initRecAnnotation(ann_b2ContactImpulse,lib);
initRecAnnotation(ann_b2World,lib);
initRecAnnotation(ann_b2Jacobian,lib);
initRecAnnotation(ann_b2JointEdge,lib);
initRecAnnotation(ann_b2JointDef,lib);
initRecAnnotation(ann_b2Joint,lib);
initRecAnnotation(ann_b2DistanceJointDef,lib);
initRecAnnotation(ann_b2DistanceJoint,lib);
initRecAnnotation(ann_b2FrictionJointDef,lib);
initRecAnnotation(ann_b2FrictionJoint,lib);
initRecAnnotation(ann_b2GearJointDef,lib);
initRecAnnotation(ann_b2GearJoint,lib);
initRecAnnotation(ann_b2MotorJointDef,lib);
initRecAnnotation(ann_b2MotorJoint,lib);
initRecAnnotation(ann_b2MouseJointDef,lib);
initRecAnnotation(ann_b2MouseJoint,lib);
initRecAnnotation(ann_b2PrismaticJointDef,lib);
initRecAnnotation(ann_b2PrismaticJoint,lib);
initRecAnnotation(ann_b2PulleyJointDef,lib);
initRecAnnotation(ann_b2PulleyJoint,lib);
initRecAnnotation(ann_b2RevoluteJointDef,lib);
initRecAnnotation(ann_b2RevoluteJoint,lib);
initRecAnnotation(ann_b2WeldJointDef,lib);
initRecAnnotation(ann_b2WeldJoint,lib);
initRecAnnotation(ann_b2WheelJointDef,lib);
initRecAnnotation(ann_b2WheelJoint,lib);
